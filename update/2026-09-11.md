# Update 11 กันยายน 2026

- ดึง Dose จาก PDF ใหม่ `Dose supportive new.pdf` (45 หน้า) เป็น `Dose supportive.csv` — 55 ยา
- ข้อความไทยใน CSV ประกอบตามลำดับตัวอักษรใน PDF ไม่เรียงตาม x เพื่อไม่ให้สระลอย
- คอลัมน์ `Page` = หน้าแรกที่ยานั้นเริ่ม (ใช้เปิด `#page=N`) หน้าต่อของยาเดียวกันถูกเย็บเข้าแถวเดิม ไม่มีแถวซ้ำต่อหน้า
- ตารางน้ำหนักที่เป็นรูปในคอลัมน์ Dose เด็ก แปะเข้าแถวยาแล้ว:
  - Paracetamol หน้า 8
  - Ibuprofen หน้า 11 (ยาเริ่มหน้า 10 ทั้งสองรูปเป็นตารางชุดเดียวกัน)
- AAFP.md มีการเพิ่มข้อมูลใหม่ **Note on Thai Clinical Practice:** 
- สร้าง `chunks.jsonl` ใหม่ทั้งชุด: AAFP.md 38 + URI.md 94 + Dose CSV 102 = **234 chunks**
- Embed ลง **Qdrant local** (`rag/qdrant_db`, collection `pharmacy_docs`) ครบ 234/234 แล้วลบของเก่าก่อนใส่ใหม่
- Qdrant Cloud ลงแล้วช่วงบ่าย ดู `update/2026-09-11-cloud.md`

## ไฟล์ที่แตะ

| สถานะ | ไฟล์ | ทำอะไร |
|---|---|---|
| ใหม่ | `backend/dose_pdf_to_csv.py` | สคริปต์ดึงตารางจาก Dose PDF → CSV (ไทยไม่เพี้ยน + แปะตารางน้ำหนักที่เป็นรูป) |
| แก้ | `backend/config.py` | Dose PDF ชี้ไป `Dose supportive new.pdf` |
| แก้ | `backend/embed_to_qdrant.py` | เลือก local/cloud ได้ + `reset_collection` |
| แก้ | `rag/pipeline.py` | เพิ่ม `--dose-pdf` / `--chunk-only` ไม่ลบทั้ง `qdrant_db` ตอน reset |
| แก้ | `frontend/react-app/src/components/PdfPanel.tsx` | เปิด PDF Dose ไฟล์ใหม่ |
| แก้ | `requirements.txt` | เพิ่ม `pdfplumber`, `pandas` |
| แก้ | `rag/data/AAFP.md` | เพิ่ม Note on Thai Clinical Practice (ไซนัส) |
| แก้ | `rag/data/Dose supportive.csv` | สร้างใหม่จาก PDF ใหม่ 55 ยา |
| ใหม่ | `rag/data/Dose supportive new.pdf` | ต้นฉบับ Dose 45 หน้า |
| ลบ | `rag/data/Dose supportive.pdf` | ไฟล์ Dose เก่า |
| ใหม่ | `rag/data/Dose supportive.bak.csv` | backup CSV ก่อนเขียนทับ |
| แก้ | `rag/data/chunks.jsonl` | chunk ใหม่ทั้งชุด 234 ชิ้น |
| แก้ | `rag/qdrant_db/` | index local `pharmacy_docs` ใหม่ 234 docs |
| แก้ | `rag/embed_log.txt` | log การ embed รอบนี้ |
| ใหม่ | `update/2026-09-11.md` | ไฟล์สรุปนี้ |
