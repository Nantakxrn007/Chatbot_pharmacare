# Update 11 กันยายน 2026 — Qdrant Cloud

- คลัสเตอร์ Cloud ถูกพักอยู่ก่อน ต่อไม่ได้ (TLS reset) หลัง reactivate เชื่อมได้แล้ว
- Embed `chunks.jsonl` ลง **Qdrant Cloud** collection `pharmacy_docs` ครบ **234/234** (ลบชุดเก่า 229 จุด แล้วใส่ใหม่)
- ไม่แตะ `chat_memory` ยังเป็น 423 จุดเหมือนเดิม
- Local (`rag/qdrant_db`) อัปเดตไปแล้วตั้งแต่เช้า ชุดเดียวกัน 234 จุด

## ไฟล์ที่แตะ

| สถานะ | ไฟล์ | ทำอะไร |
|---|---|---|
| แก้ | Qdrant Cloud `pharmacy_docs` | index ใหม่ 234 docs (AAFP 38 + URI 94 + Dose 102) |
| ไม่แก้ | Qdrant Cloud `chat_memory` | คง 423 จุด |
| แก้ | `rag/embed_log.txt` | log การ embed รอบ Cloud |
| ใหม่ | `update/2026-09-11-cloud.md` | ไฟล์สรุปนี้ |
